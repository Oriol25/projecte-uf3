export type Person = {
    name: String,
    email: String,
    tel: String
}

export type Letter = {
    letter: String,
    status: String
}

export type RowLetter = Letter[]

export type LetterPress = {
    code: String,
    key: String
};